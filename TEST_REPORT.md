# 验收记录

日期：2026-09-16

## 已通过

- TypeScript 类型检查通过。
- 静态构建通过，生成 `dist/client/index.html`。
- 题库检查通过：3 道题、48 条 WAV 音轨、48 份 MusicXML，每声部恰有一个原作选项。
- 自动穷举规格覆盖 768 种四声部组合。干扰项在每一时点保留原作音高类，因此组合不改变垂直和声内容。
- 浏览器完整流程通过：试听不会自动选中，选中状态可见，切换题目正常，12 个声部选齐后可提交，结果计分和每声部对照正常。
- 播放器的快速“播放 → 暂停 → 继续”回归测试通过；恢复位置会夹在 0 与音轨时长之间，不再产生负数偏移。
- Verovio 6.2.0 成功在揭晓后加载 MusicXML，显示女高音、女低音、男高音和男低音四行谱面。
- 320、375、390、430、768、1440 像素视口均无整页横向溢出。320–430 像素为 2×2 选项布局，768 及 1440 像素为每行四选项。
- 375 像素及以上所有可见主要按钮高度不小于 44 像素；320 像素的可见主操作也保持 44 像素，自动计算中的 0 来自隐藏控件。
- GitHub Pages 子路径 `/bach-chorale-puzzle` 构建通过，HTML 内的 CSS 与 JavaScript 资源均使用该前缀。
- 移动端音频素材包含 48 条 96 kbps 单声道 MP3，并保留 48 条 WAV 作为解码回退；MP3 总量约 9 MB。
- 音频播放器支持移动端同步解锁、旧版 `webkitAudioContext`、MP3→WAV 回退、8 项 LRU 缓存、旧请求取消和错误重试。
- 构建阶段成功生成并校验 768 份四声部 SVG 乐谱；移动视口只请求当前组合 SVG，没有请求约 7.5 MB 的 Verovio 模块。
- 390×844 移动视口完整流程通过：MP3 单声部试听、12 个选择、提交、组合播放、暂停、继续、用户组合谱与巴赫原谱切换均正常，控制台无错误或警告。
- 移动流程实际请求的 MP3 与两份静态 SVG 均返回成功；GitHub Pages 子路径下未出现资源地址错误。

## 边界与未实机项

- 已用浏览器视口模拟验证手机布局，未在真实 iPhone Safari 或 Android Chrome 上完成实机播放。
- 音频资源的加载、解码、暂停与统一起音流程已在浏览器中运行；本次没有伪造“音乐学专家人工审听”结论。上线后仍建议使用耳机和手机扬声器各完成一轮人工抽听。
- Verovio 浏览器包体较大，桌面端在揭晓后动态加载；移动端使用预生成 SVG，动态雕刻失败时桌面端也会自动回退到 SVG。
- 当前电脑没有完整 Xcode 和 iOS Simulator；真实 iPhone Safari、iPhone 微信、Android Chrome 与 Android 微信仍需发布后的真机验收，不能由移动视口模拟代替。
